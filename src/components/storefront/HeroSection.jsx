// Port from design/index.html .hero — type crop + silhouette figures + caption box
export function HeroSection() {
  return (
    <section className="relative min-h-[680px] bg-off overflow-hidden">
      {/* TODO: hero-type, hero-figures, hero-cap */}
    </section>
  );
}
